// header
#include <stdio.h>

// global variable definition
int marks = 85;

// entry point function
int main (void)
{
	// code

	if (marks >= 90)
	{
		printf (" Grade: A\n");
	}
	else if (marks >= 75)
	{
		printf ("Grade: B\n");
	}
	else if (mark >= 50)
	{
		printf("Grade: c\n");
	}
	else
	{
		printf(" Grade: F\n");
	}

	return(0);
}

