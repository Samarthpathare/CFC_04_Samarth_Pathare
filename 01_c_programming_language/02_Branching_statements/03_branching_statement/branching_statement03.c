// Headers
#include <stdio.h>

// global variable defination

int age = 20; // Age of the person 

// entry point function

int main (void)
{
	// code

	if (age >= 18)
	{
		printf ("you are eligibal to vote.\n");
	}
	else
	{
		printf("you are not eligible to vote.\n");
	}
	return(0);
}